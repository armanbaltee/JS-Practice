const balance = 1000;

if (balance <= 500) {
    console.log(`Balance is ${balance}`);
}else if(balance <= 700){
    console.log(`Balance is ${balance}`);
}else{
    console.log(`Balance is ${balance}`);
}
const month = 6;
switch (month) {
    case 1:
        console.log(`Month is Jan ${month}`)
        break;
    case 2:
        console.log(`Month is Feb ${month}`)
        break;
    case 3:
        console.log(`Month is March ${month}`)
        break;
    case 4:
        console.log(`Month is April ${month}`)
        break;
    case 5:
        console.log(`Month is May ${month}`)
        break;
    case 6:
        console.log(`Month is Jun ${month}`)
        break;

    default:
        break;
}
